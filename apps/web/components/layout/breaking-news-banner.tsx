// This will be populated from the API in Phase 2 (breaking news feature)
// For now it renders nothing — avoids an unnecessary API call on every page load

export function BreakingNewsBanner() {
  return null;
}
